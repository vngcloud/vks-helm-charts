# Default values for vks-blockstorage-csi-driver.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: vcr.vngcloud.vn/81-vks-public/vngcloud-blockstorage-csi-driver
  # Overrides the image tag whose default is v{{ .Chart.AppVersion }}
  tag: ""
  pullPolicy: IfNotPresent

# -- Custom labels to add into metadata
customLabels:
  {}

sidecars:
  provisioner:
    env: []
    image:
      pullPolicy: IfNotPresent
      repository: vcr.vngcloud.vn/81-vks-public/csi-provisioner
      tag: "v5.3.0"
    logLevel: 2
    # Additional parameters provided by external-provisioner.
    additionalArgs: []
    # Grant additional permissions to external-provisioner
    additionalClusterRoleRules:
    resources:
      limits:
        memory: 256Mi
      requests:
        cpu: 10m
        memory: 40Mi
    # Tune leader lease election for csi-provisioner.
    # Leader election is on by default.
    leaderElection:
      enabled: true
      # Optional values to tune lease behavior.
      # The arguments provided must be in an acceptable time.ParseDuration format.
      # Ref: https://pkg.go.dev/flag#Duration
      # leaseDuration: "15s"
      # renewDeadline: "10s"
      # retryPeriod: "5s"
    securityContext:
      readOnlyRootFilesystem: true
      allowPrivilegeEscalation: false
      seccompProfile:
        type: RuntimeDefault
  attacher:
    env: []
    image:
      pullPolicy: IfNotPresent
      repository: vcr.vngcloud.vn/81-vks-public/csi-attacher
      tag: "v4.11.0"
    # Tune leader lease election for csi-attacher.
    # Leader election is on by default.
    leaderElection:
      enabled: true
      # Optional values to tune lease behavior.
      # The arguments provided must be in an acceptable time.ParseDuration format.
      # Ref: https://pkg.go.dev/flag#Duration
      # leaseDuration: "15s"
      # renewDeadline: "10s"
      # retryPeriod: "5s"
    logLevel: 2
    # Additional parameters provided by external-attacher.
    additionalArgs:
      - --timeout=6m
    # Grant additional permissions to external-attacher
    additionalClusterRoleRules: []
    resources:
      limits:
        memory: 100Mi
      requests:
        cpu: 10m
        memory: 40Mi
    securityContext:
      readOnlyRootFilesystem: true
      allowPrivilegeEscalation: false
      seccompProfile:
        type: RuntimeDefault
  snapshotter:
    # Enables the snapshotter sidecar even if the snapshot CRDs are not installed
    forceEnable: false
    env: []
    image:
      pullPolicy: IfNotPresent
      repository: vcr.vngcloud.vn/81-vks-public/csi-snapshotter
      tag: "v8.5.0"
    logLevel: 2
    # Additional parameters provided by csi-snapshotter.
    additionalArgs: []
    # Grant additional permissions to csi-snapshotter
    additionalClusterRoleRules: []
    resources:
      limits:
        memory: 256Mi
      requests:
        cpu: 10m
        memory: 40Mi
    securityContext:
      readOnlyRootFilesystem: true
      allowPrivilegeEscalation: false
      seccompProfile:
        type: RuntimeDefault
  livenessProbe:
    image:
      pullPolicy: IfNotPresent
      repository: vcr.vngcloud.vn/81-vks-public/csi-livenessprobe
      tag: "v2.18.0"
    # Additional parameters provided by livenessprobe.
    additionalArgs: []
    resources:
      limits:
        memory: 32Mi
      requests:
        cpu: 10m
        memory: 32Mi
    securityContext:
      readOnlyRootFilesystem: true
      allowPrivilegeEscalation: false
      seccompProfile:
        type: RuntimeDefault
  resizer:
    env: []
    image:
      pullPolicy: IfNotPresent
      repository: vcr.vngcloud.vn/81-vks-public/csi-resizer
      tag: "v2.1.0"
    # Tune leader lease election for csi-resizer.
    # Leader election is on by default.
    leaderElection:
      enabled: true
      # Optional values to tune lease behavior.
      # The arguments provided must be in an acceptable time.ParseDuration format.
      # Ref: https://pkg.go.dev/flag#Duration
      # leaseDuration: "15s"
      # renewDeadline: "10s"
      # retryPeriod: "5s"
    logLevel: 2
    # Additional parameters provided by external-resizer.
    additionalArgs: []
    # Grant additional permissions to external-resizer
    additionalClusterRoleRules: []
    resources:
      limits:
        memory: 256Mi
      requests:
        cpu: 10m
        memory: 40Mi
    securityContext:
      readOnlyRootFilesystem: true
      allowPrivilegeEscalation: false
      seccompProfile:
        type: RuntimeDefault
  nodeDriverRegistrar:
    env: []
    image:
      pullPolicy: IfNotPresent
      repository: vcr.vngcloud.vn/81-vks-public/csi-node-driver-registrar
      tag: "v2.16.0"
    logLevel: 2
    httpEndpoint:
    # Additional parameters provided by node-driver-registrar.
    additionalArgs:
      - --http-endpoint=0.0.0.0:9809
    resources:
      limits:
        memory: 100Mi
      requests:
        cpu: 10m
        memory: 40Mi
    securityContext:
      readOnlyRootFilesystem: true
      allowPrivilegeEscalation: false
      seccompProfile:
        type: RuntimeDefault
    # /healthz kiem tra registration socket (<kubeletPath>/plugins_registry/<driver>-reg.sock)
    # con ton tai va con phuc vu gRPC; tra 404 khi socket bi xoa -> kubelet restart sidecar
    # -> registrar dang ky lai -> CSINode co lai driver.
    # KHONG dung --mode=kubelet-registration-probe: tu v2.16.0 no la no-op (exit 0),
    # xem kubernetes-csi/node-driver-registrar#309. Yeu cau --http-endpoint o additionalArgs.
    livenessProbe:
      httpGet:
        path: /healthz
        port: 9809
      initialDelaySeconds: 30
      periodSeconds: 30
      timeoutSeconds: 5
      successThreshold: 1
      failureThreshold: 3
  volumemodifier:
    env: []
    image:
      pullPolicy: IfNotPresent
      repository: vcr.vngcloud.vn/81-vks-public/vngcloud-csi-volume-modifier
      tag: "v1.0.0"
    leaderElection:
      enabled: true
      # Optional values to tune lease behavior.
      # The arguments provided must be in an acceptable time.ParseDuration format.
      # Ref: https://pkg.go.dev/flag#Duration
      # leaseDuration: "15s"
      # renewDeadline: "10s"
      # retryPeriod: "5s"
    logLevel: 2
    # Additional parameters provided by volume-modifier-for-k8s.
    additionalArgs: []
    resources:
      limits:
        memory: 256Mi
      requests:
        cpu: 10m
        memory: 40Mi
    securityContext:
      readOnlyRootFilesystem: true
      allowPrivilegeEscalation: false
      seccompProfile:
        type: RuntimeDefault

proxy:
  http_proxy:
  no_proxy:

imagePullSecrets: []
nameOverride: ""
fullnameOverride: "vngcloud-csi"

vngcloudAccessSecret:
  name: vngcloud-csi-secret
  keyId: key_id                                        # PUT YOUR ClientID here
  accessKey: access_key                                # PUT YOUR ClientSecret here
  identityEndpoint: "https://iamapis.vngcloud.vn/accounts-api"
  vserverEndpoint: "https://hcm-3.api.vngcloud.vn/vserver/vserver-gateway"
  vksClusterId: "cluster_id"                              # PUT YOUR ClusterID here

controller:
  volumeModificationFeature:
    enabled: true
  # Additional parameters provided by vngcloud-blockstorage-csi-driver controller.
  additionalArgs: []
  loggingFormat: text
  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            topologyKey: kubernetes.io/hostname
  # The default filesystem type of the volume to provision when fstype is unspecified in the StorageClass.
  # If the default is not set and fstype is unset in the StorageClass, then no fstype will be set
  defaultFsType: ext4
  env: []
  # Use envFrom to reference ConfigMaps and Secrets across all containers in the deployment
  envFrom: []
  # If set, add pv/pvc metadata to plugin create requests as parameters.
  extraCreateMetadata: true
  # Extra volume tags to attach to each dynamically provisioned volume.
  # ---
  # extraVolumeTags:
  #   key1: value1
  #   key2: value2
  extraVolumeTags: {}
  httpEndpoint:
  # (deprecated) The TCP network address where the prometheus metrics endpoint
  # will run (example: `:8080` which corresponds to port 8080 on local host).
  # The default is empty string, which means metrics endpoint is disabled.
  # ---
  enableMetrics: false
  # When metrics are enabled, put prometheus.io/scrape and prometheus.io/port on
  # the metrics Service.
  #
  # This is the only discovery path that works WITHOUT the prometheus-operator
  # CRDs. Until now the chart shipped a ServiceMonitor and nothing else, so a
  # tenant running plain Prometheus - or any agent that discovers by annotation -
  # could enable metrics and still scrape nothing, with no error to tell them
  # why. aws-ebs-csi-driver ships both and defaults this to true; so do we.
  enablePrometheusAnnotations: true
  serviceMonitor:
    # Enables the ServiceMonitor resource even if the prometheus-operator CRDs are not installed
    forceEnable: false
    # Additional labels for ServiceMonitor object
    labels:
      release: prometheus
  # If set to true, VngCloud API call metrics will be exported to the following
  # TCP endpoint: "0.0.0.0:3301"
  # ---
  # ID of the Kubernetes cluster used for tagging provisioned VngCloud volumes (optional).
  k8sTagClusterId:
  logLevel: 5
  userAgentExtra: "vks"
  nodeSelector: {}
  deploymentAnnotations: {}
  podAnnotations: {}
  podLabels: {}
  priorityClassName: system-cluster-critical
  region:
  replicaCount: 2
  revisionHistoryLimit: 10
  socketDirVolume:
    emptyDir: {}
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 1
  # type: RollingUpdate
  # rollingUpdate:
  #   maxSurge: 0
  #   maxUnavailable: 1
  resources:
    requests:
      cpu: 10m
      memory: 40Mi
    limits:
      memory: 256Mi
  serviceAccount:
    # A service account will be created for you if set to true. Set to false if you want to use your own.
    create: true
    name: ""
    annotations: {}
    automountServiceAccountToken: true
  terminationGracePeriodSeconds: 30
  tolerations:
    - key: CriticalAddonsOnly
      operator: Exists
    - key: components.vks.greennode.ai/vks-managed-components
      operator: Exists
    - effect: NoSchedule
      key: kubernetes.io/arch
      operator: Equal
      value: arm64
    - effect: NoExecute
      key: node.kubernetes.io/not-ready
      operator: Exists
      tolerationSeconds: 300
    - effect: NoExecute
      key: node.kubernetes.io/unreachable
      operator: Exists
      tolerationSeconds: 300
  topologySpreadConstraints: []
  # securityContext on the controller pod
  securityContext:
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 1000
    fsGroup: 1000
    seccompProfile:
      type: RuntimeDefault
  # Add additional volume mounts on the controller with controller.volumes and controller.volumeMounts
  volumes:
    - name: add-hosts
      hostPath:
        path: /etc/hosts
  # Add additional volumes to be mounted onto the controller:
  # - name: custom-dir
  #   hostPath:
  #     path: /path/to/dir
  #     type: Directory
  volumeMounts:
    - name: add-hosts
      mountPath: /etc/hosts
  # And add mount paths for those additional volumes:
  # - name: custom-dir
  #   mountPath: /mount/path
  # ---
  # securityContext on the controller container (see sidecars for securityContext on sidecar containers)
  containerSecurityContext:
    readOnlyRootFilesystem: true
    allowPrivilegeEscalation: false
    seccompProfile:
      type: RuntimeDefault
  initContainers: []

node:
  # Bound on the preStop hook, which drains this node's VolumeAttachments before the
  # node plugin dies so NodeUnstage still has a plugin to run against. kubelet
  # force-kills the pod here, so this IS the hook's only timeout.
  #
  # 90 rather than Kubernetes' default of 30: draining a node with 8 volumes was
  # measured at 43.8s for all attachments to clear (TS-C3, dev cluster 09/09/2026),
  # and the hard cap is 10 volumes per node (VOLUME_PER_SERVER quota), which
  # extrapolates to roughly 55s. 60 would leave almost no headroom.
  #
  # The cost is paid only on a real drain: the hook returns immediately when the node
  # carries no drain taint, so rolling restarts and driver upgrades are unaffected.
  terminationGracePeriodSeconds: 90
  env: []
  envFrom: []
  kubeletPath: /var/lib/kubelet
  loggingFormat: text
  logLevel: 5
  priorityClassName: system-node-critical
  additionalArgs: []
  affinity: {}
  nodeSelector: {}
  daemonSetAnnotations: {}
  podAnnotations: {}
  podLabels: {}
  tolerateAllTaints: true
  tolerations:
    - operator: Exists
      effect: NoSchedule
    - operator: Exists
      effect: NoExecute
  resources:
    requests:
      cpu: 10m
      memory: 40Mi
    limits:
      memory: 256Mi
  revisionHistoryLimit: 10
  probeDirVolume:
    emptyDir: {}
  serviceAccount:
    create: true
    name: ""
    annotations: {}
    automountServiceAccountToken: true
  # Enable the linux daemonset creation
  enableLinux: true
  # The "maximum number of attachable volumes" per node.
  #
  # Left empty, the driver asks vServer for the VOLUME_PER_SERVER quota in
  # NodeGetInfo and reports that. Set this only to override a wrong quota.
  #
  # node.reservedVolumeAttachments was removed: it rendered
  # --reserved-volume-attachments, a flag this driver does not have. The flag
  # set is built with ExitOnError, so setting that value made the node plugin
  # exit on startup and the whole DaemonSet CrashLoop. It came from
  # aws-ebs-csi-driver, where the flag exists.
  volumeAttachLimit:
  # Serve the driver's Prometheus metrics from the node plugin as well.
  #
  # The node plugin talks to vServer too - NodeGetInfo calls GetPortalInfo and
  # GetQuotaByName - and until now none of that was observable: on a 15-node
  # cluster that is 15 processes calling the IaaS with no metrics at all. The
  # driver has always supported this; only the chart never passed the flag.
  enableMetrics: false
  # See controller.enablePrometheusAnnotations. Same reasoning, and the node
  # Service needs it more: a DaemonSet has no single Service a human would
  # think to point a scrape config at.
  enablePrometheusAnnotations: true
  serviceMonitor:
    # Enables the ServiceMonitor resource even if the prometheus-operator CRDs are not installed
    forceEnable: false
    # Additional labels for ServiceMonitor object
    labels:
      release: prometheus
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: "10%"
  hostNetwork: false
  # securityContext on the node pod
  securityContext:
    # The node pod must be run as root to bind to the registration/driver sockets
    runAsNonRoot: false
    runAsUser: 0
    runAsGroup: 0
    fsGroup: 0
  # Add additional volume mounts on the node pods with node.volumes and node.volumeMounts
  volumes:
    - name: add-hosts
      hostPath:
        path: /etc/hosts
  # Add additional volumes to be mounted onto the node pods:
  # - name: custom-dir
  #   hostPath:
  #     path: /path/to/dir
  #     type: Directory
  volumeMounts:
    - name: add-hosts
      mountPath: /etc/hosts
  # And add mount paths for those additional volumes:
  # - name: custom-dir
  #   mountPath: /mount/path
  # ---
  # securityContext on the node container (see sidecars for securityContext on sidecar containers)
  containerSecurityContext:
    readOnlyRootFilesystem: true
    privileged: true
  # Enable opentelemetry tracing for the plugin running on the daemonset
  otelTracing: {}
  #  otelServiceName: ebs-csi-node
  #  otelExporterEndpoint: "http://localhost:4317"

additionalDaemonSets:
# Additional node DaemonSets, using the node config structure
# See docs/additional-daemonsets.md for more information
#
# example:
#   nodeSelector:
#     node.kubernetes.io/instance-type: c5.large
#   volumeAttachLimit: 15

# Enable compatibility for the A1 instance family via use of an AL2-based image in a separate DaemonSet
# a1CompatibilityDaemonSet: true

storageClasses:
  # Add StorageClass resources like:
  - name: vngcloud-ssd-3000-delete
    enabled: true
    labels:
      mininum-size: "1Gi"
      maximum-size: "5000Gi"
      throughput: "200MBps"
      iops: "3000"
      volume-type: "SSD"
    volumeBindingMode: WaitForFirstConsumer
    reclaimPolicy: Delete
    allowVolumeExpansion: true
    parameters:
      type: "ssd-iops3000"

  - name: vngcloud-nvme-5000-delete
    enabled: true
    annotations:
      storageclass.kubernetes.io/is-default-class: "true"
    labels:
      mininum-size: "1Gi"
      maximum-size: "10000Gi"
      throughput: "400MBps"
      iops: "5000"
      volume-type: "NVME"
    volumeBindingMode: WaitForFirstConsumer
    reclaimPolicy: Delete
    allowVolumeExpansion: true
    parameters:
      type: "nvme-iops5000"

volumeSnapshotClasses: []

helmTester:
  enabled: true
  # Supply a custom image to the ebs-csi-driver-test pod in helm-tester.yaml
  image: "gcr.io/k8s-staging-test-infra/kubekins-e2e:v20231206-f7b83ffbe6-master"